{
  "name": "await-timer",
  "version": "0.0.1",
  "type": "module",
  "main": "./build/awaitTimer.js",
  "types": "./build/types.d.ts",
  "scripts": {
    "dev": "vite",
    "build:types": "tsc -p ./tsconfig.json",
    "build": "vite build && pnpm build:types",
    "preview": "vite preview"
  },
  "keywords": [
    "await",
    "timer",
    "loop",
    "promise",
    "setInterval",
    "setTimeout"
  ],
  "devDependencies": {
    "typescript": "~5.7.2",
    "vite": "^6.2.0"
  }
}
